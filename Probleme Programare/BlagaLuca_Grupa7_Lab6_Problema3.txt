#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>

int main(void) {
    int a, n, a_n;
    scanf("%d", &a);
    scanf("%d", &n);
    a_n = pow(a, n);
    printf("%d la puterea %d=%d", a, n, a_n);
    return 0;






}